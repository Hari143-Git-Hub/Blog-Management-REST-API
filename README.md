# Blog-Management-REST-API
A Spring Boot-based Blog Management REST API that supports CRUD operations for posts, categories, and comments. It uses Spring Data JPA, H2 database, DTO for structured responses, pagination for efficient data handling, and follows layered architecture for clean design.
